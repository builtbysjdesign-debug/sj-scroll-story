(function() {
  // Prevent duplicate load
  if (document.querySelector('.sj-scroll-story')) return;

  // ===== INJECT HTML =====
  const html = `
  PASTE THE ENTIRE <section class="sj-scroll-story"> ... </section>
  FROM YOUR FILE HERE
  `;
  
  const container = document.createElement('div');
  container.innerHTML = html;
  document.body.appendChild(container.firstElementChild);

  // ===== INJECT CSS =====
  const style = document.createElement('style');
  style.innerHTML = `
  PASTE EVERYTHING INSIDE <style> ... </style>
  FROM YOUR FILE HERE
  `;
  document.head.appendChild(style);

  // ===== RUN YOUR JS =====
  PASTE EVERYTHING INSIDE <script> ... </script>
  FROM YOUR FILE HERE
})();
