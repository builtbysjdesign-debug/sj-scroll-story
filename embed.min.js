(function() {
  // Prevent duplicate injection
  if (document.querySelector('.sj-scroll-story')) return;

  // Inject CSS
  const style = document.createElement('style');
  style.innerHTML = `
  /* PASTE YOUR ENTIRE <style> CONTENT FROM vertical-slides-sj.html HERE */
  `;
  document.head.appendChild(style);

  // Inject HTML
  const wrapper = document.createElement('div');
  wrapper.innerHTML = `
  <!-- PASTE YOUR ENTIRE SLIDER HTML MARKUP HERE (no <style>, no <script>) -->
  `;
  document.body.appendChild(wrapper);

  // PASTE YOUR EXISTING JS LOGIC HERE (remove <script> tags)
})();

